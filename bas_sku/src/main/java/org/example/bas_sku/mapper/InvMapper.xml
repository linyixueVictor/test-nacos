<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.bas_sku.mapper.InvMapper">
    <update id="descStock">
        update bas_sku
        set qty = qty - #{qty}, editWho = #{editWho}, editTime = now()
        where sku = #{sku}
          and qty > #{qty}
    </update>
</mapper>